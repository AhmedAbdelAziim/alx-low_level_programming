#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 *@a: the input
 *Return: absoulte value.
 */
int _abs(int a)
{
	if (a >= 0)
	{
		return (a);
	}
	else
	{
		return (a * -1);
	}
}
